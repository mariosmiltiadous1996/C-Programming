#include <stdio.h>

main(){
	int x;
	printf("Dwse ena akeraio: ");
	scanf("%d",&x);
	
	
	if(x%2==0)
		{printf("O akeraios pou edoses einai artios!");	}
	else
		{printf("O akeraios pou edoses einai perittos!"); }
}
